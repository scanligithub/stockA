name: Test Industry Chain API

on:
  workflow_dispatch:

jobs:
  run-tests:
    runs-on: ubuntu-latest # 🛡️ 已修正：runs-ok 改为 runs-on
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: 'pip'

      - name: Install Quant Dependencies
        run: |
          pip install --upgrade pip
          pip install pandas pyarrow lxml html5lib

      - name: Test Sina HTML Parser
        run: python tests/test_sina_mainbus.py
