name: Test GBBQ HTTP Speed

on:
  workflow_dispatch: # 手动触发

jobs:
  http-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
          
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          
      - name: Initialize Test Environment
        run: |
          go mod init test_gbbq_http || true
          go get github.com/injoyai/tdx
          go mod tidy
          
      - name: Run GBBQ HTTP Diagnostic
        run: |
          go run scripts/test_gbbq_http.go
